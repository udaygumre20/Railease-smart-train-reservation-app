// ============================================================
// Theme Types
// ============================================================

/** User-selectable theme preference. */
export type ThemeMode = 'light' | 'dark' | 'system';

/** The resolved (active) theme after system preference resolution. */
export type ResolvedTheme = 'light' | 'dark';
